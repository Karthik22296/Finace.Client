/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface LedgerGetByCustomer$Params {
  customerId: number;
  loanId?: number | null;
  fromDate?: string | null;
  toDate?: string | null;
}

export function ledgerGetByCustomer(http: HttpClient, rootUrl: string, params: LedgerGetByCustomer$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, ledgerGetByCustomer.PATH, 'get');
  if (params) {
    rb.path('customerId', params.customerId, {});
    rb.query('loanId', params.loanId, {});
    rb.query('fromDate', params.fromDate, {});
    rb.query('toDate', params.toDate, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/octet-stream', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

ledgerGetByCustomer.PATH = '/api/ledger/customer/{customerId}';
