/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ReportsDailyCollection$Params {
  date?: string;
  branchId?: number | null;
  collectorId?: string | null;
}

export function reportsDailyCollection(http: HttpClient, rootUrl: string, params?: ReportsDailyCollection$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, reportsDailyCollection.PATH, 'get');
  if (params) {
    rb.query('date', params.date, {});
    rb.query('branchId', params.branchId, {});
    rb.query('collectorId', params.collectorId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/octet-stream', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

reportsDailyCollection.PATH = '/api/reports/daily-collection';
