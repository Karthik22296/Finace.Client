name: CI

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build, Test & Quality Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run Tests
        run: npm run test -- --watch=false --browsers=ChromeHeadless --code-coverage
        continue-on-error: true # Tests won't fail the build if not fully set up

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@fd88b7d7ccbaefd23d8f36f73b59db7a3d246602 # v6.0.0
        continue-on-error: true # Prevents build failure if Sonar secrets are pending setup
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Build Application
        run: npm run build

  code-review:
    name: Code Review
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    uses: Karthik22296/.github/.github/workflows/pr-review.yml@232c9b92ce8cc784945111e21913e2f8e4e7020f # main
    secrets:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

