# Finace.Client — Localized Repository Context

> **Auto-generated by the Bootstrap Engine (AGENTS.md §3)**
> This file is the instance-specific projection of the universal base spec.
> All universal protocols are **inherited from [AGENTS.md](./AGENTS.md)** — this file only records discovered, repo-specific state.

---

## 1. Discovered Technology Stack

| Layer | Technology | Version |
| :--- | :--- | :--- |
| Framework | Angular | ^19.2.0 |
| UI Library | Angular Material | ^19.2.19 |
| CDK | Angular CDK | ^19.2.19 |
| Language | TypeScript | ~5.7.2 |
| API Generator | ng-openapi-gen | ^1.0.5 |
| Zone | zone.js | ~0.15.0 |
| Test Runner | Karma | ~6.4.0 |
| Test Framework | Jasmine | ~5.6.0 |
| Build System | @angular-devkit/build-angular:application | — |

---

## 2. Verified Non-Interactive Commands

All sub-agents must use these **exact** commands for verification (per AGENTS.md §2.4):

| Purpose | Command | Exit Criteria |
| :--- | :--- | :--- |
| Production Build | `npm run build` | Exit code `0`. Output in `dist/finance.client`. |
| Headless Unit Tests | `npm test -- --watch=false --browsers=ChromeHeadless` | Non-interactive; zero failures. |
| API Client Generation | `npm run generate-api` | Regenerates `src/api/` from `src/swagger.json`. |

---

## 3. API Contract & Interface Boundaries

- **OpenAPI Spec (Source of Truth)**: `src/swagger.json`
- **Generated Client Target**: `src/api/`
  - **Zero Manual Edits** — any manual change is overwritten by `npm run generate-api` (AGENTS.md §2.13 Never Do).
- **Imports**: Always use barrel files:
  - Models: `import { ... } from '../api/models';`
  - Services: `import { ... } from '../api/services';`

---

## 4. Project Structure

```
src/
├── api/              # Auto-generated (ng-openapi-gen) — NEVER edit manually
├── app/
│   ├── core/         # Singletons: guards, interceptors, auth services
│   ├── layout/       # Shell components: header, sidebar, navigation
│   ├── pages/        # Feature page components (smart containers)
│   └── shared/       # Reusable dumb components, pipes, directives
├── environments/     # environment.ts, environment.prod.ts
└── custom-theme.scss # Angular Material monochrome theme
```

---

## 5. Ephemeral Task Memory (`.agents/memory/`)

Per AGENTS.md §2.7 and §2.14:
- `active-task.json` — Task checkpoint for interruption recovery; cleared on completion.
- `trace-<taskId>.jsonl` — Phase-by-phase execution trace; retained 7 days.
- `task-history.jsonl` — Append-only cross-task learning log.

---

## 6. Active Rules & Knowledge

> Rules and knowledge files are **dynamically discovered** per AGENTS.md §4.
> The Meta-Agent scans `.agents/rules/` and `.agents/knowledge/` directories.
> No hardcoded file lists — agents discover by directory content and headers.

### Rules (`.agents/rules/`)
*(Stack-specific invariants scaffolded by the Bootstrap Engine)*

### Knowledge (`.agents/knowledge/`)
*(Domain-specific trap logs populated during task execution)*

---

## 7. Business Domain & Project Plans

- **100-Day Microfinance Application Plan**: [docs/IMPLEMENTATION_PLAN.md](../docs/IMPLEMENTATION_PLAN.md)
- **Project Tasks & Tracking**: [docs/TASKS.md](../docs/TASKS.md)

---

## 8. Companion Repositories

*(Active when present in the parent workspace directory)*
- **Backend (.NET 9 Web API)**: `../Finance.ServerApi`
- **Database (SQL Server & EF Core Migrations)**: `../Finance.GoldenDb`
