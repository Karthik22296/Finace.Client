# Knowledge Directory

> Per AGENTS.md §4 — knowledge files are created dynamically during task execution.
> Named by content: `<domain>-<concern>-issues.md`
> Promoted to rules when issues recur ≥2 times.

This directory is intentionally empty at bootstrap. The Meta-Agent populates it as domain-specific traps are discovered during task execution.
